package model.workout;

import java.util.Set;

public class WeeklySchedule {
 
    // REQUIRES: workouts is not null, workouts has the same number of Workout objects as daysOfWeek has numbers,
    //           daysOfWeek has numbers only between 0 and 6 inclusive
    // EFFECTS: Instantiate the weekly workout schedule the user will follow with their built exercise deck. 
    public WeeklySchedule(WorkoutLibrary workouts, Set<Integer> daysOfWeek) {

    }

    // addWorkout
    // removeWorkout
}
